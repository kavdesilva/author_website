const { Book, User } = require('../models')

// functions for data retrieval, form inputs, etc. go below.

// const getAllBooks = async(req,res) => {
//   try {
//     const books = await Book.find({})
//     return books
//   } catch (error) {
//     throw error}
// }
// 08/01/2022 - STILL cannot get my controllers to work with the mern stack review, I end up in a never ending load screen. will try to refactor later. 

const getPoetryBooks = () => {
  
}

const getFictionBooks = () => {
  
}

const getBookById = () => {
  
}

const getUser = () => {
  
}

const addFavBook = () => {
  
}

const addWantBook = () => {
  
}

const getFavBooks = () => {
  
}

const getWantBooks = () => {
  
}

module.exports = {}